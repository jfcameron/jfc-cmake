# jfc-cmake

CMake modules shared by the C++ projects on this account. Vendored as a submodule at `cmake/jfc-cmake`.

```cmake
include("${CMAKE_CURRENT_SOURCE_DIR}/cmake/jfc-cmake/jfclib.cmake")
```

